# Copy to .env and fill in your Render PostgreSQL connection values.
# If left blank, the server falls back to an in-memory pg-mem database (dev only).
PGUSER=""
PGPASSWORD=""
PGHOST=""
PGPORT=
PGDATABASE=""
